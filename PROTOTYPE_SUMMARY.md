# 두루온 앱 프로토타입 구현 정리

## 현재 구현 범위

두루온은 영주 관광지를 목적지로 선택하고 DRT를 예약한 뒤, 탑승 상태와 지난 이용 내역까지 확인하는 모바일 앱 프로토타입이다. 현재 화면은 아이폰형 목업 프레임 안에서 동작하도록 맞춰 두었고, 주요 사용자 흐름은 실제 앱처럼 클릭 가능하게 연결했다.

## 사용자 앱

### 1. 로그인 및 회원가입

- 사용자 앱 로그인 화면 구현
- 임시 로그인 계정 제공
  - 아이디: `user`
  - 비밀번호: `demo1234`
- 회원가입 화면 구현
  - 이름, 전화번호, 아이디, 비밀번호 입력
  - 가입 후 바로 로그인 처리
- 로그인 세션은 `sessionStorage` 기반으로 유지
- 임시 회원 계정은 `localStorage`에 저장
- 새로고침 후 로그인 화면이 바로 나오지 않는 경우는 세션이 남아 있기 때문

### 2. 홈 화면

- 영주역/풍기역 출발 선택 구현
- 소수서원, 부석사, 무섬마을 관광지 카드 표시
- 관광공사 API 실패 배너 제거
- 로컬 환경에서는 관광지 기본 데이터와 이미지가 항상 보이도록 처리
- 관광지 선택 후 DRT 예약 화면으로 이동

### 3. DRT 예약 화면

- 출발지, 목적지, 지도, 탑승 시간, 인원 선택 구현
- 목적지 선택 전 예약 확정 시 안내 문구 표시
- 예약 화면 하단에도 홈/예약/내 탑승/마이페이지 탭바 표시
- 탑승 시간 기본 선택값은 `10분 후`
- 인원 변경에 따라 총 금액 자동 계산
- 예약 확정 시 결제 방식 선택 모달 표시

### 4. 결제 방식

- 예약 확정 전 결제 방식 선택 가능
- 현장 결제 선택 가능
- 마이페이지에서 등록한 간편결제 수단 선택 가능
- 네이버페이, 카카오페이 등록 흐름을 임시 구현
- 실제 결제 PG 연동은 아직 없음

### 5. 내 탑승 화면

- 예약 완료 후 내 탑승 화면으로 이동
- 예정된 여정 카드 표시
- 차량 도착까지 남은 시간 표시
- 남은 시간은 실제 런타임 기준으로 초 단위 감소
- 탑승 시작 버튼 구현
- 탑승 시작 후 하차 완료 버튼으로 상태 변경
- 하차 완료 후 홈으로 가기 버튼 표시
- 홈으로 가기 후 해당 예약은 예정된 여정에서 제거
- 완료된 여정은 지난 탑승 내역 맨 위에 추가

### 6. 지난 탑승 내역

- 기본 예시 내역 표시
- 완료된 예약이 지난 내역 첫 번째로 들어가도록 구현
- 지난 내역 클릭 시 상세 정보 표시
- 상세에는 출발지, 도착지, 이용 시간, 결제 방식, 금액 표시

### 7. 마이페이지

- 사용자 정보 카드 표시
- 프로필 수정 안내 패널 구현
- 결제수단 관리 구현
- 이용가이드, 공지사항, 고객센터 상세 패널 구현
- 각 상세 패널은 선택한 메뉴 바로 아래에 표시되도록 수정
- 로그아웃 구현

## 관리자 대시보드

관리자 대시보드는 별도 프로젝트로 구성되어 있다.

- 경로: `/Users/mac/Documents/ChatGPT/앱 개발/yeongju-drt-admin`
- 관리자 로그인 화면 구현
- 임시 관리자 계정 구성
  - `admin / demo1234`
  - `operator / demo1234`
- 실제 관리자 DB는 추후 Supabase Auth 또는 별도 관리자 계정 테이블로 확장 가능

## API 및 데이터 구조

### 관광 정보 API

로컬 CRA 개발 서버에서는 `/api/tour` 서버리스 함수가 붙지 않는다. 이 환경에서는 API 응답 대신 `index.html`이 돌아와서 관광 정보 로딩 실패처럼 보일 수 있다.

현재 처리 방식:

- API는 백그라운드에서만 호출
- 실패해도 사용자 화면에는 오류 배너를 띄우지 않음
- 기본 관광지 데이터와 이미지를 로컬 데이터로 표시

배포 시 필요한 작업:

- Vercel 배포
- `TOUR_API_KEY` 환경변수 설정
- Vercel 서버리스 함수 `/api/tour` 동작 확인

### 로그인 및 DB 확장 방향

현재는 공모전 MVP용 임시 저장 구조다.

- 사용자 로그인: `sessionStorage`
- 임시 회원가입 정보: `localStorage`
- 간편결제 등록 정보: `localStorage`
- 예약 및 탑승 정보: React state

추후 확장 방향:

- 사용자 인증: Supabase Auth
- 사용자 프로필: `user_profiles`
- 예약 정보: `reservations`
- 결제수단 메타데이터: `payment_methods`
- 탑승 기록: `ride_histories`
- 운영 설정 및 노선 정보: Postgres 테이블

## 대표 시나리오

### 시나리오 1. 관광지 선택 후 DRT 예약

1. 로그인
2. 홈에서 출발역 선택
3. 관광지 카드 선택
4. DRT 호출하기
5. 탑승 시간과 인원 선택
6. 예약 확정하기
7. 현장 결제 또는 간편결제 선택
8. 내 탑승 화면에서 예약 완료 확인

### 시나리오 2. 탑승 진행 및 완료

1. 내 탑승 화면 진입
2. 남은 도착 시간 확인
3. 탑승 시작
4. 하차 완료
5. 홈으로 가기
6. 내 탑승 화면 재진입
7. 예정된 여정에서 사라진 것 확인
8. 지난 탑승 내역 맨 위에 추가된 것 확인

### 시나리오 3. 결제수단 등록 후 예약

1. 마이페이지 진입
2. 결제수단 관리 선택
3. 네이버페이 또는 카카오페이 등록
4. 홈으로 이동
5. 관광지 선택 후 예약
6. 예약 확정 단계에서 등록한 간편결제 선택
7. 내 탑승 화면에서 결제 방식 확인

## 팀원 공유 방법

가장 적합한 공유 방식은 Vercel 배포 링크다.

- Figma 안에 React 앱 코드를 직접 삽입해서 실행하는 방식은 일반적인 공유 방식이 아님
- Figma에는 배포 링크, QR 코드, 주요 화면 캡처, 대표 시나리오를 붙이는 방식이 적합
- 팀원에게는 배포 링크를 보내고, Figma에는 프로토타입 설명용 자료를 정리하면 된다

## 현재 검증된 항목

- `npm run build` 통과
- 로그인 및 회원가입 흐름 확인
- 홈 관광지 카드 표시 확인
- 예약 화면 하단 탭바 표시 확인
- 예약 확정 후 결제 방식 선택 확인
- 내 탑승 실시간 도착 카운트다운 확인
- 탑승 시작 및 하차 완료 상태 변경 확인
- 홈으로 가기 후 예정 여정 제거 확인
- 완료 여정이 지난 탑승 내역으로 이동하는 흐름 확인
- 마이페이지 상세 카드가 선택 항목 바로 아래 표시되는지 확인

## 남은 작업

- Vercel 배포
- 관광공사 API 키 환경변수 연결
- Supabase Auth 연동
- 예약, 탑승, 결제수단 DB 테이블 설계
- 관리자 대시보드와 사용자 예약 데이터 연결
- 실제 결제 PG 연동 여부 결정
- 발표용 화면 캡처 및 QR 코드 제작
